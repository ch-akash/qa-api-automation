1. @Before hook will run before each scenario
2. @After hook will run after each scenario
3. @BeforeStep will run before each step
4. @AfterStep will run after each step
5. @Before(order=1) will run before each scenario with order 1
6. @After(order=1) will run after each scenario with order 1
3. Hooks combined with a tag will run only for the scenarios with that tag
4. Hooks can be used to setup and teardown the test environment
5. Hooks can be used to setup and teardown the DB connections
8. Hooks can be used to setup and teardown the test data